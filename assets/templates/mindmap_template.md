# 《{课程名}》思维导图
```mermaid
mindmap
  root(({课程名}))
    框架/方法
      ...
    原则/公式
      ...
    例题
      ...
    易错点
      ...
    术语
      ...
```

> 说明：
> - 用 mermaid `mindmap` 语法；若运行环境不支持渲染，改用同级缩进列表（根 = 课程名，一级分支同上五类）。
> - 分支节点只放知识点短名；依赖与对比关系用带标签的 `-->` 注记在图后单独列出，如图后"关系注记"一节：
>
> ## 关系注记
> - {知识点A} 是 {知识点B} 的前置
> - {知识点C} 与 {知识点D} 易混，区别在 {分歧点}
